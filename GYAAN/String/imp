for (char c : String.valueOf(cnt).toCharArray()) // make cnt string then make it char array to iterate


// if 2 string has samefreq of characters and and all same characters then by swapping charcters we cna achieve one another as its all about permutation..
https://leetcode.com/problems/determine-if-two-strings-are-close/description/
//
 String repeated = sb.toString().repeat(n / len); repeats sb n/len times // useful for checking if reoeating things comesup

//Try to iterate on string backward also..

//
if (sub.compareTo(best) < 0) best = sub;
it means if sub (our current substring) is lexicogrsphically smaller than best then replace it with sub.. if returns >0 means current string is lexicrographically greter 
//////////////
Number of Substrings With Only 1s 
//https://leetcode.com/problems/number-of-substrings-with-only-1s/description/?envType=daily-question&envId=2025-11-16
